version: 0.2

phases:
    install:
        runtime-versions:
            nodejs: 10
        commands:
            - echo "installing something"
    pre_build:
        commands:
            - echo "we are in the pre build phase"
    build:
        commands:
            - echo "Running SQL file"
            - bash run_sql.sh $TRAN_RUNTIME
    post_build:
        commands:
            - echo "we are in the post build phase"